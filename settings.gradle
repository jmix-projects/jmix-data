rootProject.name = 'jmix-data'

include 'data'
include 'eclipselink'
include 'eclipselink-starter'
include 'data-hibernate'
include 'data-hibernate-starter'

rootProject.children.each { p1 ->
	p1.buildFileName = "${p1.name}.gradle"
}
